package com.coditas.frontline.repository;

import com.coditas.frontline.entity.TeamAgent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TeamAgentRepository extends JpaRepository<TeamAgent, Long> {
}
